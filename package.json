{
  "name": "da-board",
  "version": "2.1.3",
  "author": "mgrybyk",
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --force-local",
    "clean": "lerna run --parallel clean",
    "build": "lerna run --restrict-to=client build silent",
    "setup": "run-s clean bootstrap build"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5"
  }
}
